import { FilterAsignacionPipe } from './filter-asignacion.pipe';

describe('FilterAsignacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAsignacionPipe();
    expect(pipe).toBeTruthy();
  });
});
