import { FilterDocentePipe } from './filter-docente.pipe';

describe('FilterDocentePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDocentePipe();
    expect(pipe).toBeTruthy();
  });
});
